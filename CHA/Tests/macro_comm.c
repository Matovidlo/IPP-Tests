#define void single(double * state, char ch); /*
*/ void single(double * state, char ch);

int triple(int c, ...);

#define void single(double * state, char ch); //\
void single(double * state, char ch);

int quattro(type c, ...);